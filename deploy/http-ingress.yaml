apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: http
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: http
            port:
              number: 80
